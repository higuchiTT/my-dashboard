# Dashboard Project

## プロジェクト概要
`dashboard.html` はビルドツール・外部ライブラリなしの単一ファイル個人ダッシュボード。
新機能はすべてこのファイル内で完結させること。外部ファイルを追加しない。

## 着手前のルール

依頼内容が曖昧で、実装方針に複数の解釈余地がある場合は **必ず AskUserQuestion で質問してから着手する**。
勝手に推測して実装を進めない。具体的には以下が不明瞭なときは質問する：
- 位置・配置・順序（どこに置く、何の隣に、どの順で）
- UI/UX の挙動（常時表示か・クリックで表示か、編集方法）
- 既存機能をどうするか（残す・廃止・置き換え）
- 幅・サイズ・優先度（固定か可変か、何pxか）
- データモデル変更の伴う場合の移行方針

## dashboard.html を編集したら必ずやること
1. 下記マッピングで **確認が必要な項目を絞り込む**
2. **checker エージェント**を呼び、絞り込んだ項目だけを確認するよう指示する
3. checker の結果を根拠に、確認した項目を「✅ 確認済み」または「⚠ 修正した」で明示してから完了報告する

### 変更タイプ → 確認項目マッピング

| 変更タイプ | 確認する項目 |
|-----------|------------|
| 列幅・グリッド変更 | 3（カラム幅）、8（最小幅） |
| クリック・編集動作の変更 | 1（クリック領域）、2（入力欄幅）、6（stopPropagation） |
| 要素の追加・削除 | 1（クリック領域）、6（stopPropagation）、7（不要要素） |
| 色・フォント・余白のみ変更 | 4（行の高さ）、5（数値右揃え） |
| 行の高さ・パディング変更 | 4（行の高さ） |
| 複数タイプにまたがる変更 | 該当するすべての項目 |
| JS関数にIDを渡す実装を追加・変更 | 9（ID型ミスマッチ） |
| 判断できない場合 | 全項目 |

## エージェント構成

| エージェント | 役割 | 呼び出しタイミング |
|-------------|------|------------------|
| `coder` | dashboard.html の実装 | 変更・追加・修正を依頼するとき |
| `checker` | 視覚確認 + checklist 検証 | coder が実装完了したとき |

**メイン会話がこの順で呼ぶ**: coder → checker → Stop Hook（自動）

ページ構成・データ構造・コーディング規約・デザイントークンなど実装時の詳細は `.claude/agents/coder.md` 参照。
Hook・MCP・ファイル構成の全体像は `.claude/architecture.md` 参照。

## 構成変更時のルール

エージェント・Hook・MCP の構成を変更した場合は、**必ず `.claude/architecture.md` も合わせて更新すること**。
対象となる変更例：
- エージェントの追加・削除・役割変更（`.claude/agents/`）
- Hook の追加・削除・トリガー条件の変更（`.claude/hooks/`、`settings.json`）
- MCP サーバーの追加・削除（`.mcp.json`、`settings.json`）
- checklist.md の項目追加・削除
